/*
	소코반 게임

	1. 파일에서 스테이지 정보를 읽어온다. 
	   스테이지 정보에는 스테이지 명, 맵 정보가 있다.
	
	2. 맵은 #, P, O, o 가 표시 되어 있다. 
	   # 은 벽, P는 플레이어, O 는 o 가 
	   위치해야 하는 목적지이다. 

	3. 플레이어는 o 를 O 로 옮겨야 하며, 
	   벽으로는 움직일 수 없다. 
	   o 를 O 로 옮기면 0으로 변경 되며, 
	   맵에 모든 O 가 0으로 변경되면 다음 스테이지로 넘어간다.
*/


#include<iostream>

int main() 
{

	return 0;
}